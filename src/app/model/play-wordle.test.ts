import { scoreUserGuess } from "./play-wordle";

describe("Play Wordle", () => {
  describe("Guess Checker (t:ANVIL)", () => {
    it.todo("should work with correct guesses (g:ANVIL)");
    it.todo("handles mixed guesses correctly (g:AINGE)");
    it.todo("should handle extra misplaced letters correctly (g:HELLO)");
    it.todo("should handle extra correct letters correctly (g:SPELL)");
    it.todo("Any other relevant cases?");
  });
  describe("Game", () => {
    it.todo("how will we test this?");
  });
});
